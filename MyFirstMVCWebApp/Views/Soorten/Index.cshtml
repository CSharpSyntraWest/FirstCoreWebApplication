@model IEnumerable<MyFirstMVCWebApp.Models.Soorten>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Soort)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Soort)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SoortNr">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SoortNr">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SoortNr">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
